clear all;
close all;
clc;

img_color = imread('C:\...\lena_color.png');
figure;
imshow(img_color);

[h, w, c] = size(img_color)

pix10_10 = img_color(10,10,:) % : -> toate canalele

figure;
subplot(1,3,1), imshow(img_color(:,:,1)); % canalul R - toate liniile - toate coloanele
subplot(1,3,2), imshow(img_color(:,:,2)); % canalul G
subplot(1,3,3), imshow(img_color(:,:,3)); % canalul B

img_color(:,:,2) = 0; %G = 0
img_color(:,:,3) = 0; %B = 0
figure;
imshow(img_color);